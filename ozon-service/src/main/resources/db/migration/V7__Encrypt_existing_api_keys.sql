-- Шифруем существующие API ключи
-- Это будет выполнено автоматически при следующем сохранении через JPA
-- так как CryptoStringConverter автоматически шифрует данные при сохранении

-- Для существующих записей, которые могут быть незашифрованными,
-- при следующем обновлении они будут автоматически зашифрованы

-- Примечание: Эта миграция не изменяет данные напрямую,
-- так как шифрование происходит на уровне JPA при сохранении
